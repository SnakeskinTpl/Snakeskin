/**!
 * @status stable
 * @version 1.0.0
 */

Snakeskin.addDirective(
	'with',

	{
		placement: 'template',
		sys: true
	},

	function (command) {
		this.startDir();
		this.scope.push(command);
	},

	function () {
		this.scope.pop();
	}
);