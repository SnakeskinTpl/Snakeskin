/**!
 * @status stable
 * @version 1.0.0
 */

Snakeskin.addDirective(
	'&',

	{
		inBlock: true
	},

	function () {
		this.startInlineDir();
		if (this.isSimpleOutput()) {
			this.space = true;
		}
	}
);