/**!
 * @status stable
 * @version 1.0.0
 */

Snakeskin.addDirective(
	'&',

	{
		placement: 'template'
	},

	function () {
		this.startInlineDir();
		if (this.isSimpleOutput()) {
			this.space = true;
		}
	}
);