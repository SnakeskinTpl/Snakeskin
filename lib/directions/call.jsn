/*!
 * @status stable
 * @version 1.0.0
 */

Snakeskin.addDirective(
	'call',

	{
		inBlock: true
	},

	function (command) {
		dir.startInlineDir();
		if (dir.isSimpleOutput()) {
			dir.save('__SNAKESKIN_RESULT__ += ' + command + ';');
		}
	}
);